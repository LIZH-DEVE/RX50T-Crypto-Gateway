# RX50T UART Parser Probe

## 目标

在已经稳定的 `UART Echo` 生存线上，插入最小 `parser` 作为第二阶段板级探针。

## 输入帧格式

- `0x55`
- `LEN`
- `PAYLOAD[LEN]`

## 串口输出规则

- 合法帧：只回传 payload，并在末尾追加换行 `0x0A`
- 非法帧：输出 `E`，再追加换行 `0x0A`

## 示例

输入：

- `55 03 41 42 43`

输出：

- `41 42 43 0A`

非法输入：

- `55 00`

输出：

- `45 0A`

